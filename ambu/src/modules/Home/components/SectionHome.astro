---
import video from "../../../assets/video/Fondo.mp4";
---

<section>
  <video autoplay loop>
    <source src={video} type="video/mp4" />
    Tu navegador no soporta la etiqueta de video.
  </video>
  <div class="capa"></div>
  <h2>VIVÍ LA RECUPERACIÓN EN CUALQUIER PARTE DEL PAÍS</h2>

  <p>CONTACTANOS</p>
</section>

<style lang="scss">
  section {
    position: relative;
    height: 600px;
    video {
      width: 100%;
      height: 100%;
      opacity: 1;
      object-fit: cover;
    }

    p {
      cursor: pointer;
      color: #fff;
      font-size: 25px;
      padding: 18px;
      text-align: center;
      border-radius: 8px;
      background-color: #2b561a;
      position: absolute;
      top: 70%;
      left: 50%;
      transform: translate(-50%, -50%);
      transition: 0.2s ease;
      box-shadow: 0 10px 40px 20px #0000004f;

      &:hover {
        box-shadow: 0 20px 50px 20px #0000004f;
        scale: 1.05;
      }
    }

    h1 {
      font-size: 20px;
      color: #fff;
      text-align: center;
      position: absolute;
      top: 5%;
      left: 50%;
      transform: translate(-50%, -50%);
    }
    h2 {
      width: 700px;
      font-size: 30px;
      text-align: center;
      color: #fff;
      position: absolute;
      top: 42%;
      left: 50%;
      transform: translate(-50%, -50%);
    }

    .capa {
      position: absolute;
      top: 0;
      height: 100%;
      width: 100%;
      background: radial-gradient(circle, #ffffff5c, #1c1c1d);
      opacity: 0.9;
    }
  }
</style>
