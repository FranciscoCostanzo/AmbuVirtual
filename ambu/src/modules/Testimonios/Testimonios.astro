---
import felipe from "../../assets/video/Testimonios/FelipeTestimonio.mp4"
import manzu from "../../assets/video/Testimonios/ManzuTestimonio.mp4"
import mauri from "../../assets/video/Testimonios/MauriTestimonio.mp4"
import hector from "../../assets/video/Testimonios/HectorTestimonio.mp4"

interface Testimonio {
  nombre: string;
  videoSrc: string;
}


const arrayTestimonios: Testimonio[] = [
  { nombre: 'Felipe de Paz', videoSrc: felipe },
  { nombre: 'Carlos Manzu', videoSrc: manzu },
  { nombre: 'Mauricio Decunta', videoSrc: mauri },
  { nombre: 'Hector Patrignani', videoSrc: hector },
];
---

<section id="testimonios" class="testimonios-section">
  <h2>Testimonios</h2>
  <div class="testimonios-container">
    {arrayTestimonios.map((testimonio) => (
      <div class="testimonio">
        <h3>Paciente: {testimonio.nombre}</h3>
        <video controls>
          <source src={testimonio.videoSrc} type="video/mp4" />
          Your browser does not support the video tag.
        </video>
      </div>
    ))}
  </div>
</section>

<style lang="scss">
  .testimonios-section {
    background-color: #f5f5f5;
    padding: 40px 20px;
    text-align: center;
    color: #333;

    h2 {
      font-size: 32px;
      color: #2b561a;
      margin-bottom: 20px;
    }

    .testimonios-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
    }

    .testimonio {
      background-color: #437031;
      border-radius: 8px;
      padding: 20px;
      width: 300px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      color: #f5f5f5;
      
      h3 {
        font-size: 20px;
        margin-bottom: 10px;
      }

      video {
        width: 100%;
        border-radius: 8px;
      }
    }
  }
</style>
